{
  "name": "@cem/server-inventory",
  "version": "1.4.0",
  "description": "Read-only machine inventory for CEM: natively collects host/CPU/disks, GPU (incl. Windows sysmem-fallback VRAM), conda, WSL, Docker, installed programs, CLIs and config scripts, then derives threshold-based alerts. Ported from the standalone 'lupa_servidor' tool.",
  "type": "module",
  "license": "MIT",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@cem/shared": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.3.5",
    "typescript": "^5.7.3"
  }
}
